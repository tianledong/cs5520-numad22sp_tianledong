<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.numad22sp_tianledong">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_td"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_td_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD22Sp_TianleDong">
        <activity
            android:name="edu.neu.madcourse.numad22sp_tianledong.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="edu.neu.madcourse.numad22sp_tianledong.ClickyActivity" />
    </application>

</manifest>